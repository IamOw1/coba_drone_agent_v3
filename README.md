# 🚁 COBA AI Drone Agent 2.0 (Orchids Remix)

Интеллектуальная система управления дроном с ИИ-агентом, основанная на архитектуре из документа "Полная система управления дроном с ИИ-агентом".

## 📋 Содержание

- [Описание](#описание)
- [Архитектура](#архитектура)
- [Установка](#установка)
- [Использование](#использование)
- [Инструменты](#инструменты)
- [API](#api)
- [Дашборд](#дашборд)
- [Конфигурация](#конфигурация)
- [Разработка](#разработка)

## 📝 Описание

COBA AI Drone Agent 2.0 - это комплексная система управления дроном с использованием искусственного интеллекта. Система включает:

- **ИИ-агент** с краткосрочной и долгосрочной памятью
- **Субагент GPT-4o** для контроля и анализа
- **10+ инструментов** для различных задач
- **Обучение с подкреплением** (RL)
- **Интеграцию с симуляторами** (AirSim)
- **REST API** для внешнего управления
- **Веб-дашборд** на Streamlit

## 🏗️ Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                    COBA AI Drone Agent 2.0                  │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │ Core Agent  │  │ Sub-Agent   │  │ Decision Maker      │ │
│  │ (Основной)  │  │ (GPT-4o)    │  │ (Принятие решений)  │ │
│  └─────────────┘  └─────────────┘  └─────────────────────┘ │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │ Short-Term  │  │ Long-Term   │  │ Learner (RL)        │ │
│  │ Memory      │  │ Memory      │  │ (Обучение)          │ │
│  └─────────────┘  └─────────────┘  └─────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│                        Инструменты                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │
│  │ Amorfus  │ │ Slom     │ │ MiFly    │ │ GeoMap       │  │
│  │ (Рой)    │ │ (Безоп.) │ │ (Полет)  │ │ (Карты)      │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐  │
│  │ Precision│ │ Object   │ │ Mission  │ │ Logistics    │  │
│  │ Landing  │ │ Detection│ │ Planner  │ │ (Доставка)   │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────────┘  │
│  ┌──────────┐ ┌──────────┐                                 │
│  │ Autonomous│ │ Deployment│                                │
│  │ Flight   │ │ Manager  │                                 │
│  └──────────┘ └──────────┘                                 │
├─────────────────────────────────────────────────────────────┤
│                    Интеграции                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐ │
│  │ AirSim      │  │ REST API    │  │ Streamlit Dashboard │ │
│  │ (Симулятор) │  │ (FastAPI)   │  │ (Веб-интерфейс)     │ │
│  └─────────────┘  └─────────────┘  └─────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## 🚀 Установка

### Требования

- Python 3.9+
- 8GB+ RAM
- GPU (опционально, для ускорения ML)

### Установка зависимостей

```bash
# Клонирование репозитория
git clone <repository-url>
cd coba_drone_agent

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt
```

### Настройка окружения

```bash
# Создание .env файла
cp .env.example .env

# Редактирование .env
nano .env
```

Пример `.env`:
```
OPENAI_API_KEY=your_api_key_here
```

## 💻 Использование

### 🐳 Запуск в Docker

```bash
# Сборка образа
docker build -t coba-ai-drone .

# Запуск API + Dashboard
docker run --rm -p 8000:8000 -p 8501:8501 --env-file .env coba-ai-drone
```

Или через Docker Compose:

```bash
docker compose up --build
```


### Запуск агента (интерактивный режим)

```bash
python main.py agent
```

### Запуск API сервера

```bash
python main.py api --host 0.0.0.0 --port 8000
```

### Запуск дашборда

```bash
python main.py dashboard
```

### Запуск всего (API + Dashboard)

```bash
python main.py all
```

## 🛠️ Инструменты

### Amorfus - Роевой интеллект
Управление группой дронов с использованием алгоритмов роевого интеллекта (Вичека, Boids).

```python
# Установка строя
await agent.tools["amorfus"].action_set_formation("v_shape")

# Установка цели
await agent.tools["amorfus"].action_set_target(x=100, y=100, z=30)
```

### Slom - Безопасность
Мониторинг критических параметров и обработка аварийных ситуаций.

```python
# Проверка безопасности
result = await agent.tools["slom"].apply({"telemetry": telemetry_data})

# Установка геозоны
await agent.tools["slom"].action_set_geofence(
    center={"x": 0, "y": 0, "z": 0},
    radius=500,
    max_altitude=120
)
```

### MiFly - Базовое управление
Команды взлета, посадки, навигации.

```python
# Взлет
await agent.tools["mifly"].action_takeoff(altitude=10)

# Перемещение
await agent.tools["mifly"].action_goto(x=50, y=50, z=20, speed=5)

# Посадка
await agent.tools["mifly"].action_land()
```

### GeoMap - Картографирование
Создание карт, планирование миссий обследования.

```python
# Создание миссии обследования
result = await agent.tools["geomap"].action_create_survey_mission(
    area_name="Область 1",
    bounds={"north": 100, "south": 0, "east": 100, "west": 0},
    altitude=50
)
```

### PrecisionLanding - Точная посадка
Посадка на визуальные маркеры.

```python
# Установка цели посадки
await agent.tools["precision_landing"].action_set_target(
    x=0, y=0, marker_type="aruco"
)

# Точная посадка
await agent.tools["precision_landing"].action_precision_land()
```

### ObjectDetection - Обнаружение объектов
Детекция объектов с помощью YOLO.

```python
# Обнаружение объектов
result = await agent.tools["object_detection"].action_detect(image=image_data)

# Отслеживание объекта
await agent.tools["object_detection"].action_track_object("person")
```

### MissionPlanner - Планировщик миссий
Создание миссий из шаблонов.

```python
# Создание миссии патрулирования
result = await agent.tools["mission_planner"].action_create_mission(
    name="Патруль 1",
    mission_type="patrol",
    params={
        "points": [{"x": 0, "y": 0}, {"x": 100, "y": 0}],
        "altitude": 30
    }
)
```

### Logistics - Логистика
Управление доставкой посылок.

```python
# Регистрация посылки
await agent.tools["logistics"].action_register_package(
    package_id="PKG001",
    weight=1.5,
    dimensions={"x": 20, "y": 20, "z": 15},
    pickup_location={"x": 0, "y": 0},
    delivery_location={"x": 100, "y": 100}
)

# Доставка
await agent.tools["logistics"].action_deliver_package("PKG001")
```

### AutonomousFlight - Автономный полет
Управление автономными режимами полета.

```python
# Установка режима
await agent.tools["autonomous_flight"].action_set_flight_mode("auto")

# Навигация к точке
await agent.tools["autonomous_flight"].action_navigate_to(
    lat=55.7558, lon=37.6173, altitude=50
)
```

### DeploymentManager - Управление развертыванием
Развертывание и отзыв групп дронов.

```python
# Развертывание
await agent.tools["deployment_manager"].action_deploy(
    deployment_id="DEP001",
    template="surveillance",
    area={"x": 0, "y": 0, "width": 200, "height": 200}
)

# Отзыв
await agent.tools["deployment_manager"].action_recall("DEP001")
```

## 🔌 API

### Эндпоинты

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/v1/agent/status` | Статус агента |
| POST | `/api/v1/agent/initialize` | Инициализация |
| POST | `/api/v1/agent/shutdown` | Выключение |
| POST | `/api/v1/mission/start` | Запуск миссии |
| POST | `/api/v1/mission/stop` | Остановка миссии |
| GET | `/api/v1/mission/status` | Статус миссии |
| POST | `/api/v1/command` | Отправка команды |
| POST | `/api/v1/emergency/stop` | Аварийная остановка |
| GET | `/api/v1/telemetry` | Телеметрия |
| GET | `/api/v1/tools` | Список инструментов |
| POST | `/api/v1/tools/{name}/execute` | Выполнение инструмента |
| GET | `/api/v1/learning/progress` | Прогресс обучения |
| GET | `/api/v1/sub_agent/ask` | Вопрос субагенту |

### Примеры запросов

```bash
# Статус агента
curl http://localhost:8000/api/v1/agent/status

# Запуск миссии
curl -X POST http://localhost:8000/api/v1/mission/start \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Тестовая миссия",
    "waypoints": [
      {"x": 10, "y": 10, "z": 10},
      {"x": 20, "y": 20, "z": 10}
    ],
    "altitude": 10
  }'

# Отправка команды
curl -X POST http://localhost:8000/api/v1/command \
  -H "Content-Type: application/json" \
  -d '{
    "command": "takeoff",
    "params": {"altitude": 10}
  }'

# Вопрос субагенту
curl "http://localhost:8000/api/v1/sub_agent/ask?question=Какой статус системы?"
```

## 📊 Дашборд

Веб-интерфейс на Streamlit для управления дроном.

### Вкладки:
- **📊 Телеметрия** - Мониторинг параметров полета
- **🗺️ Миссии** - Управление миссиями
- **🎮 Команды** - Отправка команд
- **🧠 ИИ-Помощник** - Чат с субагентом
- **🎓 Обучение** - Прогресс обучения

### Запуск:
```bash
streamlit run dashboard/app.py
```

## ⚙️ Конфигурация

Файл `config/config.yaml` содержит все настройки системы:

```yaml
# Основные параметры
agent_id: "drone_agent_001"

# Режим симуляции
simulation:
  enabled: true
  simulator: "airsim"

# Безопасность
safety:
  battery_critical: 15
  battery_low: 25
  max_altitude: 120

# Обучение
learning:
  enabled: true
  algorithm: "dqn"
  learning_rate: 0.001

# Субагент
sub_agent:
  enabled: true
  model: "gpt-4o"
  api_key: "${OPENAI_API_KEY}"

# Инструменты
tools:
  - module: "slom"
    class: "SlomTool"
    enabled: true
  # ... другие инструменты
```

## 🔧 Разработка

### Структура проекта

```
coba_drone_agent/
├── agent/              # Основной агент
│   ├── core.py         # Ядро агента
│   ├── memory.py       # Система памяти
│   ├── decision_maker.py
│   ├── learner.py      # Обучение RL
│   └── sub_agent.py    # Субагент GPT-4o
├── tools/              # Инструменты
│   ├── base_tool.py
│   ├── amorfus.py
│   ├── slom.py
│   ├── mifly.py
│   └── ...
├── sim/                # Симуляторы
│   └── airsim_client.py
├── api/                # REST API
│   └── rest_api.py
├── dashboard/          # Веб-дашборд
│   └── app.py
├── utils/              # Утилиты
│   └── logger.py
├── config/             # Конфигурация
│   └── config.yaml
├── data/               # Данные
│   ├── missions/
│   ├── models/
│   ├── logs/
│   └── memory/
├── main.py             # Главный файл
├── requirements.txt
└── README.md
```

### Тестирование

```bash
# Запуск тестов
pytest tests/

# С покрытием
pytest --cov=agent tests/
```

### Линтинг

```bash
# Форматирование
black agent/ tools/ api/

# Проверка
flake8 agent/ tools/ api/
```

## 📄 Лицензия

MIT License

## 👥 Авторы

- COBA AI Team

## 🙏 Благодарности

- AirSim - Microsoft
- OpenAI - GPT-4o
- Ultralytics - YOLO
- Streamlit
