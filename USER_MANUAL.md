# 📘 Руководство пользователя COBA AI Drone Agent 2.0

## Содержание
1. [Обзор системы](#обзор-системы)
2. [Интерфейсы управления](#интерфейсы-управления)
3. [Основные функции](#основные-функции)
4. [Инструменты в деталях](#инструменты-в-деталях)
5. [Работа с миссиями](#работа-с-миссиями)
6. [Обучение агента](#обучение-агента)
7. [Субагент GPT-4o](#субагент-gpt-4o)
8. [Примеры использования](#примеры-использования)
9. [API Reference](#api-reference)

---

## Обзор системы

COBA AI Drone Agent 2.0 — это интеллектуальная система управления дроном, которая объединяет:

- **ИИ-агент** с памятью и способностью обучаться
- **10 специализированных инструментов** для разных задач
- **Субагента GPT-4o** для анализа и советов
- **REST API** для интеграции
- **Веб-дашборд** для визуального управления

### Архитектура взаимодействия

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  Пользователь│────▶│   API/      │────▶│  ИИ-агент   │
│             │     │  Dashboard  │     │             │
└─────────────┘     └─────────────┘     └──────┬──────┘
                                                │
                       ┌────────────────────────┼────────────────────────┐
                       │                        │                        │
                       ▼                        ▼                        ▼
                ┌─────────────┐          ┌─────────────┐          ┌─────────────┐
                │  Инструменты│          │   Память    │          │  Обучение   │
                │  (10 шт.)   │          │  (SQLite)   │          │    (RL)     │
                └─────────────┘          └─────────────┘          └─────────────┘
                       │
                       ▼
                ┌─────────────┐
                │  Субагент   │
                │   GPT-4o    │
                └─────────────┘
```

---

## Интерфейсы управления

### 1. Интерактивный режим (CLI)

Запуск:
```bash
python main.py agent
```

Меню:
```
==================================================
Команды:
  1. Статус        - Показать статус агента
  2. Телеметрия    - Получить данные с датчиков
  3. Команда       - Отправить команду дрону
  4. Миссия        - Создать и запустить миссию
  5. Субагент      - Задать вопрос ИИ-помощнику
  6. Выход         - Завершить работу
==================================================
```

### 2. REST API

Базовый URL: `http://localhost:8000`

Документация: `http://localhost:8000/docs` (Swagger UI)

### 3. Веб-дашборд

URL: `http://localhost:8501`

Вкладки:
- 📊 Телеметрия — мониторинг в реальном времени
- 🗺️ Миссии — создание и управление миссиями
- 🎮 Команды — быстрые команды
- 🧠 ИИ-Помощник — чат с субагентом
- 🎓 Обучение — прогресс обучения

---

## Основные функции

### Получение статуса

**Через API:**
```bash
curl http://localhost:8000/api/v1/agent/status
```

**Ответ:**
```json
{
  "agent_id": "drone_agent_001",
  "state": "ready",
  "mission": null,
  "telemetry": {
    "position": {"x": 0, "y": 0, "z": 0},
    "battery": 100.0,
    "signal_strength": 100
  },
  "tools_status": {
    "slom": "ready",
    "mifly": "ready"
  }
}
```

### Отправка команд

**Доступные команды:**
- `takeoff [высота]` — взлет
- `land` — посадка
- `rtl` — возврат на точку взлета
- `hover` — зависание
- `goto x,y,z` — полет в точку

**Через API:**
```bash
curl -X POST http://localhost:8000/api/v1/command \
  -H "Content-Type: application/json" \
  -d '{
    "command": "takeoff",
    "params": {"altitude": 15}
  }'
```

**Через CLI:**
```
Выберите действие (1-6): 3
Введите команду (взлет/посадка/rtl/зависни): взлет на 15 метров
```

### Получение телеметрии

**Через API:**
```bash
curl http://localhost:8000/api/v1/telemetry
```

**Через CLI:**
```
Выберите действие (1-6): 2

Телеметрия:
  Позиция: {"x": 10.5, "y": 20.3, "z": 15.0}
  Батарея: 87.5%
  Скорость: {"vx": 2.1, "vy": 1.5, "vz": 0.0}
  GPS: 3D_FIX
```

---

## Инструменты в деталях

### 1. Amorfus — Роевой интеллект

Управление группой дронов с использованием алгоритмов роевого интеллекта.

**Алгоритмы:**
- **Вичека** — выравнивание скорости с соседями
- **Boids** — separation, alignment, cohesion
- **Консенсус** — согласование действий роя

**Использование:**
```bash
# Установка строя
curl -X POST http://localhost:8000/api/v1/tools/amorfus/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "set_formation",
    "params": {"formation": "v_shape"}
  }'

# Доступные строи: line, circle, pyramid, v_shape, free
```

**API эндпоинты:**
- `set_formation` — установка строя
- `set_target` — установка целевой точки
- `set_leader` — назначение лидера
- `get_swarm_status` — статус роя

---

### 2. Slom — Безопасность

Мониторинг критических параметров и обработка аварийных ситуаций.

**Контролируемые параметры:**
- Уровень заряда батареи
- Сила сигнала
- Расстояние до препятствий
- Температура
- Геозона (ограничение зоны полета)

**Уровни безопасности:**
- 🟢 **NORMAL** — нормальный режим
- 🟡 **CAUTION** — требует внимания
- 🟠 **WARNING** — предупреждение
- 🔴 **CRITICAL** — критическая ситуация
- 🚨 **EMERGENCY** — аварийная ситуация

**Использование:**
```bash
# Установка геозоны
curl -X POST http://localhost:8000/api/v1/tools/slom/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "set_geofence",
    "params": {
      "center": {"x": 0, "y": 0, "z": 0},
      "radius": 500,
      "max_altitude": 120
    }
  }'

# Получение статуса безопасности
curl -X POST http://localhost:8000/api/v1/tools/slom/execute \
  -H "Content-Type: application/json" \
  -d '{"command": "get_safety_status"}'
```

---

### 3. MiFly — Базовое управление

Команды взлета, посадки, навигации.

**Команды:**
- `takeoff` — взлет на заданную высоту
- `land` — посадка
- `goto` — перемещение в точку
- `hover` — зависание
- `rtl` — возврат на точку взлета
- `set_velocity` — установка скорости
- `follow_path` — следование по маршруту

**Использование:**
```bash
# Взлет
curl -X POST http://localhost:8000/api/v1/tools/mifly/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "takeoff",
    "params": {"altitude": 20}
  }'

# Полет в точку
curl -X POST http://localhost:8000/api/v1/tools/mifly/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "goto",
    "params": {"x": 100, "y": 50, "z": 30, "speed": 5}
  }'

# Посадка
curl -X POST http://localhost:8000/api/v1/tools/mifly/execute \
  -H "Content-Type: application/json" \
  -d '{"command": "land"}'
```

---

### 4. GeoMap — Картографирование

Создание карт, планирование миссий обследования.

**Функции:**
- Добавление точек на карту
- Создание миссий обследования (змейка)
- Создание 3D-моделей (орбитальный полет)
- Сохранение карт в HTML/JSON

**Использование:**
```bash
# Создание миссии обследования
curl -X POST http://localhost:8000/api/v1/tools/geomap/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "create_survey_mission",
    "params": {
      "area_name": "Область 1",
      "bounds": {
        "north": 100,
        "south": 0,
        "east": 100,
        "west": 0
      },
      "altitude": 50,
      "speed": 5
    }
  }'

# Сохранение карты
curl -X POST http://localhost:8000/api/v1/tools/geomap/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "save_map",
    "params": {"filename": "my_map.html"}
  }'
```

---

### 5. PrecisionLanding — Точная посадка

Посадка на визуальные маркеры (ArUco, QR-коды).

**Фазы посадки:**
1. **Approach** — приближение к точке над целью
2. **Alignment** — выравнивание с маркером
3. **Descent** — контролируемое снижение
4. **Final** — финальная фаза
5. **Touchdown** — касание

**Использование:**
```bash
# Установка цели посадки
curl -X POST http://localhost:8000/api/v1/tools/precision_landing/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "set_target",
    "params": {
      "x": 0,
      "y": 0,
      "marker_type": "aruco"
    }
  }'

# Выполнение точной посадки
curl -X POST http://localhost:8000/api/v1/tools/precision_landing/execute \
  -H "Content-Type: application/json" \
  -d '{"command": "precision_land"}'
```

---

### 6. ObjectDetection — Обнаружение объектов

Детекция объектов с помощью YOLO.

**Поддерживаемые классы:**
- person (человек)
- car (автомобиль)
- building (здание)
- И многие другие...

**Использование:**
```bash
# Обнаружение объектов
curl -X POST http://localhost:8000/api/v1/tools/object_detection/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "detect",
    "params": {"confidence": 0.7}
  }'

# Установка целевых классов
curl -X POST http://localhost:8000/api/v1/tools/object_detection/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "set_target_classes",
    "params": {"classes": ["person", "car"]}
  }'

# Отслеживание объекта
curl -X POST http://localhost:8000/api/v1/tools/object_detection/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "track_object",
    "params": {"object_class": "person"}
  }'
```

---

### 7. MissionPlanner — Планировщик миссий

Создание миссий из шаблонов.

**Шаблоны:**
- **survey** — обследование территории
- **patrol** — патрулирование
- **inspection** — инспекция объекта
- **search** — поисковая операция
- **delivery** — доставка
- **photography** — аэрофотосъемка

**Использование:**
```bash
# Создание миссии патрулирования
curl -X POST http://localhost:8000/api/v1/tools/mission_planner/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "create_mission",
    "params": {
      "name": "Патруль 1",
      "mission_type": "patrol",
      "params": {
        "points": [
          {"x": 0, "y": 0, "hover_time": 5},
          {"x": 100, "y": 0, "hover_time": 5},
          {"x": 100, "y": 100, "hover_time": 5}
        ],
        "altitude": 30,
        "loops": 2
      }
    }
  }'

# Список миссий
curl -X POST http://localhost:8000/api/v1/tools/mission_planner/execute \
  -H "Content-Type: application/json" \
  -d '{"command": "list_missions"}'
```

---

### 8. Logistics — Логистика

Управление доставкой посылок.

**Использование:**
```bash
# Регистрация посылки
curl -X POST http://localhost:8000/api/v1/tools/logistics/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "register_package",
    "params": {
      "package_id": "PKG001",
      "weight": 1.5,
      "dimensions": {"x": 20, "y": 20, "z": 15},
      "pickup_location": {"x": 0, "y": 0},
      "delivery_location": {"x": 100, "y": 100},
      "priority": 1
    }
  }'

# Доставка посылки
curl -X POST http://localhost:8000/api/v1/tools/logistics/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "deliver_package",
    "params": {"package_id": "PKG001"}
  }'

# Статус доставки
curl -X POST http://localhost:8000/api/v1/tools/logistics/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "get_delivery_status",
    "params": {"package_id": "PKG001"}
  }'
```

---

### 9. AutonomousFlight — Автономный полет

Управление автономными режимами полета.

**Режимы полета:**
- **MANUAL** — ручное управление
- **STABILIZE** — стабилизация
- **ALT_HOLD** — удержание высоты
- **LOITER** — зависание
- **AUTO** — автономный полет
- **GUIDED** — управляемый полет
- **RTL** — возврат домой
- **LAND** — посадка

**Использование:**
```bash
# Установка режима
curl -X POST http://localhost:8000/api/v1/tools/autonomous_flight/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "set_flight_mode",
    "params": {"mode": "auto"}
  }'

# Навигация к точке
curl -X POST http://localhost:8000/api/v1/tools/autonomous_flight/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "navigate_to",
    "params": {
      "lat": 55.7558,
      "lon": 37.6173,
      "altitude": 50
    }
  }'

# Следование по маршруту
curl -X POST http://localhost:8000/api/v1/tools/autonomous_flight/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "follow_waypoints",
    "params": {
      "waypoints": [
        {"lat": 55.75, "lon": 37.61, "altitude": 30},
        {"lat": 55.76, "lon": 37.62, "altitude": 30}
      ],
      "loop": false
    }
  }'
```

---

### 10. DeploymentManager — Управление развертыванием

Развертывание и отзыв групп дронов.

**Шаблоны развертывания:**
- **surveillance** — наблюдение за территорией
- **perimeter** — охрана периметра
- **search** — поисковая операция
- **convoy** — сопровождение колонны

**Использование:**
```bash
# Развертывание
curl -X POST http://localhost:8000/api/v1/tools/deployment_manager/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "deploy",
    "params": {
      "deployment_id": "DEP001",
      "template": "surveillance",
      "area": {
        "x": 0,
        "y": 0,
        "width": 200,
        "height": 200
      },
      "duration": 60
    }
  }'

# Статус развертывания
curl -X POST http://localhost:8000/api/v1/tools/deployment_manager/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "get_deployment_status",
    "params": {"deployment_id": "DEP001"}
  }'

# Отзыв развертывания
curl -X POST http://localhost:8000/api/v1/tools/deployment_manager/execute \
  -H "Content-Type: application/json" \
  -d '{
    "command": "recall",
    "params": {"deployment_id": "DEP001"}
  }'
```

---

## Работа с миссиями

### Создание миссии

**Через API:**
```bash
curl -X POST http://localhost:8000/api/v1/mission/start \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Моя миссия",
    "waypoints": [
      {"x": 10, "y": 10, "z": 15},
      {"x": 20, "y": 20, "z": 15},
      {"x": 30, "y": 10, "z": 15}
    ],
    "altitude": 15,
    "speed": 5
  }'
```

**Через дашборд:**
1. Откройте вкладку "🗺️ Миссии"
2. Введите название миссии
3. Установите точки маршрута
4. Нажмите "▶️ Запустить миссию"

### Остановка миссии

```bash
curl -X POST http://localhost:8000/api/v1/mission/stop
```

### Просмотр истории

```bash
curl http://localhost:8000/api/v1/reports/missions
```

---

## Обучение агента

### Как работает обучение

Агент использует **Deep Q-Learning** для обучения:
- Состояние → Действие → Награда → Новое состояние
- Нейронная сеть предсказывает оптимальные действия
- Epsilon-greedy стратегия (исследование vs использование)

### Просмотр прогресса

```bash
curl http://localhost:8000/api/v1/learning/progress
```

**Ответ:**
```json
{
  "learning_progress": {
    "step_count": 1500,
    "episode_count": 25,
    "total_reward": 1250.5,
    "epsilon": 0.285,
    "buffer_size": 10000,
    "average_reward": 0.834
  }
}
```

### Параметры обучения

Настройте в `config/config.yaml`:
```yaml
learning:
  enabled: true
  algorithm: "dqn"
  learning_rate: 0.001
  gamma: 0.99
  epsilon: 1.0
  epsilon_min: 0.01
  epsilon_decay: 0.995
  batch_size: 64
```

---

## Субагент GPT-4o

### Возможности субагента

- **Контроль инструментов** — мониторинг взаимодействия
- **Мониторинг производительности** — отслеживание параметров
- **Контроль миссий** — анализ выполнения
- **Анализ обучения** — оценка прогресса
- **Безопасность** — проверка критических параметров
- **Генерация отчетов** — автоматические отчеты
- **Предоставление советов** — рекомендации
- **Управление бэкапами** — контроль резервных копий

### Задать вопрос

**Через API:**
```bash
curl "http://localhost:8000/api/v1/sub_agent/ask?question=Какой статус системы?"
```

**Через CLI:**
```
Выберите действие (1-6): 5
Вопрос субагенту: Какие инструменты доступны?

Ответ: Доступны следующие инструменты:
- Slom (безопасность): готов
- MiFly (управление полетом): готов
- GeoMap (картографирование): готов
...
```

**Через дашборд:**
1. Откройте вкладку "🧠 ИИ-Помощник"
2. Введите вопрос
3. Нажмите "💬 Спросить"

### Примеры вопросов

- "Какой статус системы?"
- "Какие инструменты доступны?"
- "Как улучшить производительность?"
- "Что делать при низком заряде?"
- "Проанализируй последнюю миссию"

---

## Примеры использования

### Пример 1: Простая миссия

```bash
# 1. Инициализация агента
curl -X POST http://localhost:8000/api/v1/agent/initialize

# 2. Взлет
curl -X POST http://localhost:8000/api/v1/command \
  -d '{"command": "takeoff", "params": {"altitude": 20}}'

# 3. Полет в точку
curl -X POST http://localhost:8000/api/v1/command \
  -d '{"command": "goto", "params": {"x": 50, "y": 50, "z": 20}}'

# 4. Посадка
curl -X POST http://localhost:8000/api/v1/command \
  -d '{"command": "land"}'
```

### Пример 2: Обследование территории

```bash
# 1. Создание миссии обследования
curl -X POST http://localhost:8000/api/v1/tools/geomap/execute \
  -d '{
    "command": "create_survey_mission",
    "params": {
      "area_name": "Зона А",
      "bounds": {"north": 100, "south": 0, "east": 100, "west": 0},
      "altitude": 50
    }
  }'

# 2. Запуск миссии
# (используйте полученные waypoints)
```

### Пример 3: Поиск объекта

```bash
# 1. Установка целевых классов
curl -X POST http://localhost:8000/api/v1/tools/object_detection/execute \
  -d '{"command": "set_target_classes", "params": {"classes": ["person"]}}'

# 2. Обнаружение
curl -X POST http://localhost:8000/api/v1/tools/object_detection/execute \
  -d '{"command": "detect"}'

# 3. Поиск в истории
curl -X POST http://localhost:8000/api/v1/tools/object_detection/execute \
  -d '{"command": "search_object", "params": {"object_class": "person"}}'
```

---

## API Reference

### Агент

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/api/v1/agent/initialize` | Инициализация агента |
| POST | `/api/v1/agent/shutdown` | Выключение агента |
| GET | `/api/v1/agent/status` | Статус агента |

### Миссии

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/api/v1/mission/start` | Запуск миссии |
| POST | `/api/v1/mission/stop` | Остановка миссии |
| GET | `/api/v1/mission/status` | Статус миссии |

### Команды

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| POST | `/api/v1/command` | Отправка команды |
| POST | `/api/v1/emergency/stop` | Аварийная остановка |

### Телеметрия

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/v1/telemetry` | Получение телеметрии |

### Инструменты

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/v1/tools` | Список инструментов |
| POST | `/api/v1/tools/{name}/execute` | Выполнение инструмента |

### Обучение

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/v1/learning/progress` | Прогресс обучения |

### Субагент

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/v1/sub_agent/ask` | Вопрос субагенту |

### Отчеты

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/v1/reports/missions` | Отчеты о миссиях |

---

**Успешных полетов! 🚁**
