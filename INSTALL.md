# 📖 Инструкция по установке и запуску COBA AI Drone Agent 2.0

## Содержание
1. [Системные требования](#системные-требования)
2. [Установка](#установка)
3. [Настройка окружения](#настройка-окружения)
4. [Запуск системы](#запуск-системы)
5. [Проверка работы](#проверка-работы)
6. [Устранение неполадок](#устранение-неполадок)

---

## Системные требования

### Минимальные требования:
- **ОС**: Windows 10/11, Linux (Ubuntu 20.04+), macOS 12+
- **Python**: 3.9 или выше
- **RAM**: 8 GB
- **Диск**: 5 GB свободного места
- **Сеть**: Подключение к интернету (для субагента GPT-4o)

### Рекомендуемые требования:
- **ОС**: Linux Ubuntu 22.04 LTS
- **Python**: 3.11
- **RAM**: 16 GB
- **GPU**: NVIDIA с CUDA (для ускорения ML)
- **Диск**: SSD 20 GB

---

## Установка

### Шаг 1: Скачивание проекта

```bash
# Клонирование репозитория (или распаковка архива)
cd /путь/куда/скачать
git clone <repository-url>
# или распакуйте архив coba_drone_agent.zip

cd coba_drone_agent
```

### Шаг 2: Создание виртуального окружения

**Windows:**
```cmd
python -m venv venv
venv\Scripts\activate
```

**Linux/Mac:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### Шаг 3: Установка зависимостей

```bash
# Обновление pip
pip install --upgrade pip

# Установка базовых зависимостей
pip install -r requirements.txt

# Дополнительно (опционально):
# Для работы с AirSim
# pip install airsim

# Для работы с реальными дронами (MAVLink)
# pip install pymavlink dronekit
```

### Шаг 4: Создание структуры данных

```bash
# Создание необходимых директорий
mkdir -p data/{missions,models,logs,maps,memory,state,backups,reports,detections}
```

---

## Настройка окружения

### Шаг 1: Создание файла .env

```bash
# Копирование примера
cp .env.example .env

# Редактирование
nano .env  # или используйте любой текстовый редактор
```

### Шаг 2: Заполнение конфигурации

Откройте файл `.env` и заполните:

```env
# Обязательно:
OPENAI_API_KEY=sk-your-openai-api-key-here

# Опционально:
AIRSIM_HOST=localhost
AIRSIM_PORT=41451
API_HOST=0.0.0.0
API_PORT=8000
DASHBOARD_PORT=8501
LOG_LEVEL=INFO
```

**Как получить OpenAI API Key:**
1. Перейдите на https://platform.openai.com/
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел "API Keys"
4. Нажмите "Create new secret key"
5. Скопируйте ключ и вставьте в `.env`

### Шаг 3: Настройка config.yaml

Откройте `config/config.yaml` и настройте параметры:

```yaml
# Укажите ID вашего агента
agent_id: "my_drone_001"

# Включите/отключите симуляцию
simulation:
  enabled: true  # false для работы с реальным дроном

# Настройте параметры безопасности
safety:
  battery_critical: 15  # %
  max_altitude: 120     # метров
  max_distance: 1000    # метров

# Включите нужные инструменты
tools:
  - module: "slom"
    enabled: true
  - module: "mifly"
    enabled: true
  # ... и т.д.
```

---

## Запуск системы

### Вариант 1: Интерактивный режим (рекомендуется для начала)

```bash
python main.py agent
```

Вы увидите меню:
```
==================================================
Команды:
  1. Статус
  2. Телеметрия
  3. Команда
  4. Миссия
  5. Субагент
  6. Выход
==================================================
```

### Вариант 2: API сервер

**Терминал 1 - Запуск API:**
```bash
python main.py api --host 0.0.0.0 --port 8000
```

**Терминал 2 - Проверка API:**
```bash
# Проверка статуса
curl http://localhost:8000/health

# Инициализация агента
curl -X POST http://localhost:8000/api/v1/agent/initialize
```

### Вариант 3: Веб-дашборд

```bash
python main.py dashboard
```

Откройте браузер: http://localhost:8501

### Вариант 4: Всё вместе

```bash
python main.py all
```

Запустит API на порту 8000 и дашборд на порту 8501.

---

## Проверка работы

### 1. Проверка API

```bash
# Корневой эндпоинт
curl http://localhost:8000/

# Ожидаемый ответ:
# {"name": "COBA AI Drone Agent API", "version": "2.0.0", "status": "active"}
```

### 2. Проверка телеметрии

```bash
curl http://localhost:8000/api/v1/telemetry
```

### 3. Тестовая команда

```bash
curl -X POST http://localhost:8000/api/v1/command \
  -H "Content-Type: application/json" \
  -d '{"command": "takeoff", "params": {"altitude": 10}}'
```

### 4. Проверка дашборда

Откройте http://localhost:8501 и убедитесь, что:
- Все вкладки загружаются
- Телеметрия отображается
- Кнопки команд работают

---

## Устранение неполадок

### Проблема: "ModuleNotFoundError"

**Решение:**
```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Переустановите зависимости
pip install -r requirements.txt
```

### Проблема: "OPENAI_API_KEY not found"

**Решение:**
```bash
# Проверьте файл .env
cat .env

# Должно быть:
OPENAI_API_KEY=sk-...

# Или установите переменную окружения:
export OPENAI_API_KEY=sk-your-key  # Linux/Mac
set OPENAI_API_KEY=sk-your-key     # Windows
```

### Проблема: "Порт уже занят"

**Решение:**
```bash
# Найдите процесс
lsof -i :8000  # Linux/Mac
netstat -ano | findstr :8000  # Windows

# Используйте другой порт
python main.py api --port 8080
```

### Проблема: "AirSim не подключается"

**Решение:**
```bash
# Проверьте, что AirSim запущен
# В конфиге включите режим симуляции
# config/config.yaml:
simulation:
  enabled: true
```

### Проблема: Ошибки в дашборде

**Решение:**
```bash
# Обновите streamlit
pip install --upgrade streamlit

# Запустите с отладкой
streamlit run dashboard/app.py --logger.level=debug
```

---

## Дополнительная информация

### Логи

Логи сохраняются в `data/logs/`:
```bash
# Просмотр последних логов
tail -f data/logs/$(date +%Y%m%d).log
```

### Конфигурация

Все настройки в `config/config.yaml`:
- Параметры безопасности
- Настройки инструментов
- Параметры обучения
- API настройки

### Обновление

```bash
# Обновление зависимостей
pip install --upgrade -r requirements.txt

# Обновление проекта
git pull  # если используете git
```

---

## Поддержка

При возникновении проблем:
1. Проверьте логи в `data/logs/`
2. Убедитесь, что все зависимости установлены
3. Проверьте конфигурацию в `.env` и `config/config.yaml`
4. Создайте issue с описанием проблемы

---

**Готово к полету! 🚁**
